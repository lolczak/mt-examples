package org.lolczak.mt.skalleh

import org.lolczak.lambda.{LambdaValue, Lit}

import scalaz.\/
import scalaz.effect.IO

object SkallehApp {

//  val exp = Lit(12)
//
//  val result: IO[(StateType, (Logs, \/[LambdaValue, Failure]))] = runEval(Map.empty, 0, eval(exp))
//
//  println(result.unsafePerformIO())

}
